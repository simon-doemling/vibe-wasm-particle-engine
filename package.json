{
  "name": "particle-simulation",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "asbuild": "asc src/assembly/physics.ts -o public/physics.wasm --importMemory --sharedMemory --initialMemory 2560 --maximumMemory 2560 --enable simd --enable threads --enable bulk-memory -O3"
  },
  "devDependencies": {
    "assemblyscript": "^0.28.13",
    "vite": "^8.0.1"
  },
  "dependencies": {
    "pixi.js": "^8.17.1"
  }
}
